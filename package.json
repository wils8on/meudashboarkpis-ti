{
  "name": "kpi-tomticket-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/dashboard.js && node --check js/auth-guard.js && node --check js/client-store.js && node --check js/private-ticket-store.js && node --check scripts/sanitize-tomticket.mjs && node --check scripts/private-firestore.mjs && node --check scripts/sync-tomticket.mjs"
  }
}
